ajax: remote: true, id: "new_link"

new action:
new.js.erb:
$('#new_link').hide().after('<%= j render("form") %>')

_form_for - remote: true

def create
respond_to do |format|
format.js

new template: create.js.erb:
$('#new_task').remove();
$('#new_link').show();
$('#thing').append('<%= j render(@project) %>')

link_to 'destroy' - remote: true

destroy action:

respond_to do |format|
format.js

new template: destroy.js.erb:
$('#edit_project <%= @project.id %>').remove();
